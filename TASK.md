# 完整首轮任务

请从零实现“对数域隐马尔可夫推断库”，用于序列建模 / 概率算法中的本地可复用模块。

1. 从零实现离散HMM库，状态数1..12、观测符号数1..16、序列最长2000；输入初始分布、转移矩阵和发射矩阵。概率须有限非负且行和在1e-12内为1，通过校验后归一化；禁止numpy/scipy。
2. 使用对数域forward/backward与log-sum-exp，返回序列对数似然、每时刻状态后验gamma和相邻状态联合后验xi；零概率按负无穷处理，整条序列不可能时返回明确结果，不能产生NaN或伪造归一分布。
3. 实现Viterbi路径及其对数概率，概率完全相等时选字典序最小完整状态序列，不能仅按最后状态编号打破平局。空观测序列定义为似然1、路径与后验均为空。
4. 实现单次Baum-Welch更新，接受多条独立观测序列，不拼接边界；未被使用的参数行保持原值，不加入隐式平滑。不可能的训练序列使整个更新拒绝，不能部分修改模型。
5. 用短序列穷举全部状态路径作独立参考验证似然、gamma、xi和Viterbi；验证可行训练集更新后对数似然不降低（注明浮点容差），长低概率序列保持有限结果。仅一次EM，不写训练平台。

验收重点：零转移、不可达状态、全相等路径、空序列、单状态、未使用行、多序列边界、极小概率、坏矩阵。

工程范围：这是全新核心库的0-1实现，初始仓库仅有运行骨架，没有业务实现。仅使用Python 3.14.7标准库，在Windows原生环境离线运行；无需安装第三方依赖。不接数据库、中间件、云API、外部程序，不使用Docker，不制作前后端或通用CLI。
交付：实现可复用库、聚焦上述语义的自动化测试和README；python demo.py须在约8秒内运行确定性小案例，打印关键正常结果和至少一种真实边界失败，便于录制十几秒视频。测试命令为python -m unittest discover -s tests -v。先阅读本仓库README和TASK.md，简短计划后集中实现，再运行针对性测试与演示；避免反复通读仓库、无关探索和无必要的长篇文档，发现实际问题时正常修复验证，不设硬性工具调用截断。
只能操作当前仓库内文件；不读取用户配置、密钥、环境变量清单或其他工作区。不要新增GitHub Actions、CI、部署、Dependabot或远程推送。一次首轮完成，不要求用户追加“继续”；合理实现取舍写入README。不要为通过测试删除需求、硬编码示例输出或宣称未实际执行的验证通过。
