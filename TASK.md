# 完整首轮任务

请实现一个在对数域计算的隐马尔可夫模型库，包含概率推断、最可能路径和一次参数更新。输入和规模都按下面的约定处理，不需要训练平台。

实现离散HMM，1..12状态、1..16观测符号、单序列<=2000；概率有限非负，每分布行和与1相差<=1e-12，校验后归一化。观察值必须为合法符号索引，禁止numpy/scipy。

用对数域forward/backward与log-sum-exp返回对数似然、gamma和xi。零概率为负无穷；不可能序列返回loglik=-inf且gamma/xi=None，不产生NaN。空序列loglik=0，路径和后验数组为空。

实现Viterbi及路径对数分数，浮点计算得到的分数精确相等时选字典序最小完整状态序列，不能只按末状态打破平局，不用近似相等改变目标。用短路径穷举参考，比较数值时另用明确容差。

实现一次Baum-Welch，训练最多16条、每条<=64项，相互独立。空序列不贡献计数；全空训练集原样返回。零占用参数行保留原值，不隐式平滑；任一不可能序列整体拒绝，输入模型不变。

状态<=3、长度<=5的小例穷举全部路径验证似然/后验/Viterbi；单次EM训练似然不得降低超过1e-9*(1+abs(old_loglik))，长低概率序列仍稳定。演示正常推断与不可能序列，除此不增加训练功能。

测试里请覆盖这些情况：零概率、不可达状态、精确平局路径、空序列/全空训练、未使用行、多序列边界、长小概率、输入模型不变。

使用 Python 3.14.7 标准库，代码要能在 Windows 原生环境离线运行。范围就是这个库，不需要第三方依赖、外部服务、Docker 或前后端。请一并写好测试和简短的 README，说明接口及运行方法。

用 `python demo.py` 运行演示，在约 8 秒内展示一个正常结果和一个实际触发的失败；用 `python -m unittest discover -s tests -v` 运行测试。随机验证采用固定种子和少量小样本即可，不做长时间压力测试，也不扩展额外的平台功能。

请在本轮完成实现和验证，不要等我回复“继续”。正常排错需要的工具调用不设次数限制。操作范围限于当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区。不要添加 Actions、CI、部署或 Dependabot，也不要向远端推送。演示结果应由代码实际计算；最后如实说明验证结果，未运行或未通过的检查也请写明。
